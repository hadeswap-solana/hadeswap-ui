import { FC } from 'react';
import { TableProps } from "../index";
import {COLLECTION_ITEM_MOBILE} from "../../../Collections.constants";
import

export const CollectionList: FC<TableProps> = ({ onRowClick, data }) => {
  return <div></div>
};