import { FC } from 'react';

import { AppLayout } from '../../components/Layout/AppLayout';

export const MyNfts: FC = () => {
  return (
    <AppLayout>
      <h1>In progress...</h1>
    </AppLayout>
  );
};
